int main() {
    decl
        str filename;
        int temp;
    enddecl
    begin
        temp = exposcall("Read",-1,filename);
        
        temp = exposcall("Exec",filename);
        if(temp == -1) then
            temp = exposcall("Write",-2,"sdsdddsdsd");
        endif;
    return 0;
    end
}